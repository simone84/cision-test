name: Nginx-deploy

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - name: Hello from Docker
        run: docker build . -t nginx:1.19.1
      